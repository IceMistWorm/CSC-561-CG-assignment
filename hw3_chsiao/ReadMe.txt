Three files are needed to make the program work: index.html, rasterize.js, and rasterize.css

Directly open the index.html file, the result of all parts 
(Render with texture, lighting, alpha and alpha textures) will directly open in a chrome browser.


